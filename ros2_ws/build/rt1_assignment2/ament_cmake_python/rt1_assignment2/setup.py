from setuptools import find_packages
from setuptools import setup

setup(
    name='rt1_assignment2',
    version='0.0.0',
    packages=find_packages(
        include=('rt1_assignment2', 'rt1_assignment2.*')),
)
